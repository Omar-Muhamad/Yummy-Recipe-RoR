<%= form_with(model: food) do |form| %>
  <% if food.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(food.errors.count, "error") %> prohibited this food from being saved:</h2>

      <ul>
        <% food.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-4">
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, class:"border border-solid" %>
  </div>

  <div class="mb-4">
    <%= form.label :measurement_unit, style: "display: block" %>
    <%= form.text_field :measurement_unit, class:"border border-solid" %>
  </div>

  <div class="mb-4">
    <%= form.label :price, style: "display: block" %>
    <%= form.text_field :price, class:"border border-solid" %>
  </div>

  <div class="mt-11 border-solid rounded-md border-2 borde  r-slate-200 p-2 bg-gray-50 hover:bg-slate-400 text-center">
    <%= form.submit %>
  </div>
<% end %>
