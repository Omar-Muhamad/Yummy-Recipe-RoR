<%= form_with(model: [recipe, recipe_food], url: create_recipe_recipe_food_path, class:"w-full") do |form| %>
  <% if recipe_food.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(recipe_food.errors.count, "error") %> prohibited this recipe_food from being saved:</h2>

      <ul>
        <% recipe_food.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-4">
    <%= form.label :quantity, style: "display: block" %>
    <%= form.number_field :quantity, class:"w-full px-4 py-3 border" %>
  </div>

  <div class="w-full px-4 py-3 border flex justify-between">
    <%= form.label :food_id, style: "display: block" %>
    <%= form.collection_select(:food_id, Food.all, :id, :name, class:"w-full px-4 py-3 border") %>
  </div>

  <div class="mt-6 mb-10 mx-auto w-fit border-slate-700 rounded-md border-2 p-2 bg-gray-50 text-slate-700 hover:bg-slate-700 hover:text-white text-center">
    <%= form.submit %>
  </div>
<% end %>
